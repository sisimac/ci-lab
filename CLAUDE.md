# ci-lab — правила работы

Учебный проект: осваиваем CI/CD и Playwright на практике. Подопытный — простое todo-приложение (`app/index.html`), интереса само по себе не представляет. Ценность в пайплайне вокруг него.

Репозиторий: `github.com/sisimac/ci-lab` · Сайт: `sisimac.github.io/ci-lab`
Путь: `~/Developer/ci-lab`

## Главное правило: не делать за Александра

**Смысл проекта — научиться, а не получить работающий пайплайн.** Александр не разработчик и выполняет все команды и правки файлов сам, потому что так запоминается.

Отсюда режим работы:

1. Даю **пошаговую инструкцию** — что скопировать, куда вставить, что запустить. Конкретно, без «настройте кэширование».
2. Александр выполняет и пишет «готово».
3. **Я проверяю сам** — читаю файлы, дёргаю GitHub API, сверяю задеплоенное с локальным. Скриншоты и копипаст вывода терминала не нужны и не запрашиваются.

**Не редактировать файлы проекта самому**, даже когда это быстрее. Исключение — если Александр явно попросил.

**Не торопить.** Не предлагать следующий шаг, пока текущий не понят. Если объяснение оказалось объёмным — остановиться и убедиться, что оно зашло, а не вываливать следующую порцию.

**Объяснять на его файлах**, а не на абстрактных примерах: «вот эта строка в твоём `playwright.yml`», а не «обычно в workflow пишут».

## Что уже сделано

Пайплайн `lint → test → build → deploy → smoke`:

| Джоба | Что делает |
|---|---|
| `lint` | ESLint |
| `test` | Playwright, три движка, `--grep-invert @smoke` |
| `build` | сборка артефакта |
| `deploy` | GitHub Pages, ручное подтверждение через `environment`, отдаёт `outputs.page_url` |
| `smoke` | `needs: deploy`, гоняет `@smoke`-тесты по живому сайту через `BASE_URL` |

Плюс защита ветки с обязательными проверками и Dependabot со сгруппированными обновлениями экшенов.

`playwright.config.ts`: три браузера, в CI две повторные попытки и один воркер, трейс при ретрае, скриншот при падении. **`retries: process.env.CI ? 2 : 0` — заготовка из ранних сессий, не осознанное решение.** Её поведение ещё не разбиралось: это материал пункта 1 плана, а не закрытый вопрос. `webServer` становится `undefined`, когда задана `BASE_URL` — иначе Playwright поднимал бы локальный сервер поверх уже развёрнутого сайта.

## План, идём по порядку

1. **Флаки-тесты и ретраи** ← следующий
2. Скорость пайплайна: шардирование, матрица джоб, кэш браузеров, замер до и после
3. Видимость результата в PR: Summary, аннотации, комментарии бота
4. Preview-окружение на каждый PR
5. Релизы и откат: теги, changelog, GitHub Release, rollback, MTTR
6. DORA-метрики на данных собственного репозитория
7. Поднять руками инструмент визуализации DORA (Four Keys / LinearB / Sleuth / DevLake), подключить к репозиторию, посмотреть живые графики — демо-данные допустимы

Вне очереди:

- **`paths-ignore` на триггере** — сейчас правка README или `dependabot.yml` гоняет полный пайплайн вместе с ручным подтверждением деплоя. Поднималось дважды, оба раза отложено. Ждёт.
- **Смоук по расписанию** через `schedule`. Ждёт.
- **Мусор в `test-results/`** от удалённых тестов про API — решено не трогать.

## Почему сделано именно так

**Смоук отдельным файлом, а не переиспользованием `todo.spec.ts`.** Три причины: там `goto('/')`, теряющий подпуть; там чистка `localStorage` на каждый тест — лезть в хранилище на проде плохая практика; смоук обязан быть минимальным, а не гонять всю логику приложения.

**Смоук только в chromium.** Кросс-браузерность — не его задача, а установка трёх движков стоит около тридцати секунд.

**Адрес через `needs.deploy.outputs.page_url`, а не хардкодом** — чтобы смоук сам ехал за фактическим адресом развёртывания.

**Джоба `smoke` намеренно НЕ в обязательных проверках защиты ветки.** У неё `needs: deploy`, а деплой идёт только на push в `main` — значит на PR она не запускается никогда. Сделай её обязательной, и любой PR повиснет навсегда. Не «забыли добавить», а осознанно.

**Разная политика обновлений у Dependabot.** Для экшенов `patterns: "*"` — пачкой, это безопасно. Для npm `dependency-type: development` поштучно — боевые зависимости так проще локализовать при поломке.

Отброшенных вариантов пайплайна нет: до развилок «А или Б» дело не доходило, конфигурация сложилась с первого захода.

## Грабли, уже собранные

**`page.goto('/')` теряет подпуть `/ci-lab/`** на GitHub Pages. Всегда `goto('./')`.

**Локальный запуск без `PLAYWRIGHT_BROWSERS_PATH=0` падает** — браузеры лежат внутри проекта, а не в системном кэше. Поэтому все скрипты в `package.json` идут с этим префиксом; вызывать `npx playwright test` напрямую не надо, есть `npm test`.

**Отступы в YAML определяют вложенность.** Проверять парсером, а не глазами:

```
node -e "const yaml=require('./node_modules/js-yaml'); console.log(Object.keys(yaml.load(require('fs').readFileSync('.github/workflows/playwright.yml','utf8'))))"
```

**ID джобы и имя проверки — разные вещи.** Переименование `job_id` после того, как задан `name:`, ломает обязательные проверки в защите ветки: правило ссылается на одно, а приходит другое.

**Рендеринг в Linux и macOS различается** — визуальные регрессионные тесты со снимками падают в CI. Такие тесты в CI пропускать с внятной причиной в `reason`. Снимки в репозитории именно `-darwin`.

**Когда в очереди несколько мержей** — подтверждать деплой только у последнего прогона, ранние отменять.

**Создание PR — задача автора, не ревьюера.** Зелёные проверки разблокируют кнопку слияния, но сами ничего не сливают.

**CD не заканчивается деплоем.** Проверка после развёртывания — часть CD, отсюда и джоба `smoke`.

## Гигиена веток

История уже показала, чем это кончается: 24 коммита, шесть локальных веток, и работа велась в `chore/group-dependabot-actions`, пока `main` отставал на четыре коммита. **После слияния PR — переключаться на `main`, подтягивать, влитую ветку удалять.**

Разобрано 24.08.2026: подтянуты 4 коммита, пять влитых веток удалены. На GitHub ветки Dependabot и `pipeline-stages` остались — если мешают, чистить там.

## Проверка

```
npm run lint          # ESLint
npm test              # Playwright, три движка, локальный сервер поднимется сам
npm run test:chromium # только chromium, быстрее
npm run report        # HTML-отчёт последнего прогона
```

**`gh` CLI на машине не установлен** — за GitHub ходить через API `curl`'ом. **Модуля `yaml` в системном Python нет** — YAML валидировать через `node` и `js-yaml` из `node_modules` проекта, как показано выше.

Статус прогонов в CI смотреть через GitHub API, а не просить скриншот:

```
curl -s "https://api.github.com/repos/sisimac/ci-lab/actions/runs?per_page=5"
```

## Язык

Названия джоб, комментарии в конфигах и сообщения коммитов — русские. Имена файлов, переменных и тегов тестов — английские.

## Git

Ветка `main`, remote по SSH. Push — по явной команде.
